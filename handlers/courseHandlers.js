const jwt = require("jsonwebtoken");

const {create} = require("../pkg/account");
const config = require("../pkg/config");
const {
    create,
} = require ("../pkg/curse")

const login = async (req, res) => {
    try {
      
    }
  };